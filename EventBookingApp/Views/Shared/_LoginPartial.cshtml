@*@using Microsoft.AspNetCore.Http
    @{ var name = Context.Session.GetString("Name");}*@
<head>
    <link href="~/plugins/css/bootstrap.css" type="text/css" rel="stylesheet" media="all">
    <link href="~/plugins/css/style.css" type="text/css" rel="stylesheet" media="all">
    <link href="~/plugins/css/font-awesome.css" rel="stylesheet">
</head>

@if (User.Identity.IsAuthenticated)
{
    <li class="nav-item">
        <a class="hvr-sweep-to-right" href="@Url.Action("InsertBooking","Booking")">BookEvent</a>
    </li>
    <li class="nav-item">
        <a class="hvr-sweep-to-right" href="@Url.Action("Index","UserAccount")">Hello @User.Identity.Name</a>
    </li>
    <li class="nav-item">
        <a class="hvr-sweep-to-right" href="@Url.Action("LogOut","UserAccount")">LogOut</a>
    </li>

    @*<li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">@User.Identity.Name <span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><a href="@Url.Action("LogOut","UserAccount")">LogOut</a></li>
                <li><a href="#">My Profile</a></li>
                <li role="separator" class="divider"></li>
            </ul>
        </li>*@
}
else
{
    @*<li class="nav-item">
            <a class="hvr-sweep-to-right" href="@Url.Action("Registration","Account")">SignUp</a>
        </li>*@
    <li>
        <a class="hvr-sweep-to-right" href="@Url.Action("Login","UserAccount")">SignIn</a>
    </li>
}
